---
- name: Freebsd playbook
  hosts: all

  vars_files:
    - default.config.yml

  pre_tasks:
    - name: Load config.yml
      include_vars: "{{ item }}"
      with_fileglob:
        - "{{ playbook_dir }}/config.yml"
      tags: ['always']

  roles:
    - { role: pkg, become: true }
    - { role: sys, become: true }
    - { role: common }
    - { role: wm }

  tasks:
    - include_tasks: tasks/asus.yml
      when: configure_asus

    - include_tasks: tasks/virtualbox.yml
      when: configure_virtualbox

    - include_task: tasks/git-ports.yml
      when: git_ports