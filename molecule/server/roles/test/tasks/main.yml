---

- name: Gather pkg facts
  package_facts:
    strategy: all
    manager: "auto"

- name: Check installed packages
  ansible.builtin.assert:
    that:
      - "'pam_yubico' in ansible_facts.packages"
      - "'lynis' in ansible_facts.packages"
      - "'rkhunter' in ansible_facts.packages"
