---

- name: Tune GPG
  lineinfile:
    dest: /home/{{ ansible_user_id }}/.zshrc
    line: "{{ item }}"
    owner: "{{ ansible_user_id }}"
    state: present
  with_items:
    - "GPG_TTY=$(tty)"
    - "export GPG_TTY"
  when: ohmyzsh.changed
