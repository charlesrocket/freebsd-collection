---

argument_specs:
  main:
    short_description: Manages PF
    description:
      - Deploy C(pf) configurations.

    author:
      - charlesrocket

    options:
      pf_pflog:
        description: Enable C(pflog).
        type: bool
        default: false

      pf_pflog_flags:
        description: Set C(pflog) flags.
        type: str
        default: ''
