---

- name: Ensure packages are installed
  community.general.pkgng:
    state: "{{ pkgng_state }}"
    name: "{{ packages }}"
    autoremove: True
    cached: False
  vars:
    packages: "{{ pkgng_packages }}"
  become: True
